name: .NET Create Release Call

on:
  workflow_dispatch:

jobs:
  create-release:
    uses: trifork/cheetah-infrastructure-utils/.github/workflows/dotnet-create-release.yml@main
    with:
      csproj-filepath: cheetah-dotnet-release-test.csproj
    secrets:
      PAT: ${{ secrets.PACKAGE_PAT }}