name: Create and publish Cheetah.WebApi.Shared nuget package

on:
  workflow_dispatch:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]

jobs:
  build-and-package-nuget:
    uses: ./.github/workflows/nuget-publish.yml
    with:
        project_filepath: ./src/Cheetah.WebApi.Shared/Cheetah.WebApi.Shared.csproj
        dotnet-version: "6.0"
        github_token: ${{ secrets.GITHUB_TOKEN }}
        releasenotes_path: ./src/Cheetah.WebApi.Shared/ReleaseNotes.md
        kamstrup_gitlab_token: ${{ vars.KAMSTRUP_GITLAB_TOKEN }}
  
